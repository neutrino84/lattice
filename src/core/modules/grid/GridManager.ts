
import Core from '../..'
import Module from '../Module'

export default class GridManager extends Module {
  constructor(core: Core) {
    super(core)
  }

  public init(): void {
    super.init()
  }

  public destroy(): void {
    super.destroy()
  }
}